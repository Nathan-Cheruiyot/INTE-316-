import scipy.integrate as spi
import numpy as np
def f(x):
    return np.sin(x)
a = 0
b = np.pi
integral, error = spi.quad(f, a, b)
print(f"The integral of sin(x) from 0 to π is approximately {integral}")
